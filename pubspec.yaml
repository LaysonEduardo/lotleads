name: lotleads
description: my leads  application.
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.0.0  
  rounded_loading_button: ^2.0.3
  animated_button: ^0.2.0
  cupertino_icons: ^1.0.2
  ndialog: ^4.1.0
    
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/background/
  uses-material-design: true


